<%= form_for(tarjeta_credito) do |f| %>
  <% if tarjeta_credito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarjeta_credito.errors.count, "error") %> prohibited this tarjeta_credito from being saved:</h2>

      <ul>
      <% tarjeta_credito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :Estado %>
    <%= f.check_box :Estado %>
  </div>

  <div class="field">
    <%= f.label :creado_por %>
    <%= f.number_field :creado_por %>
  </div>

  <div class="field">
    <%= f.label :actualizado_por %>
    <%= f.number_field :actualizado_por %>
  </div>

  <div class="field">
    <%= f.label :creado %>
    <%= f.date_select :creado %>
  </div>

  <div class="field">
    <%= f.label :actualizado %>
    <%= f.date_select :actualizado %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
