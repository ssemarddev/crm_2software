json.extract! detallecaja, :id, :caja_id, :cantidad, :tipo, :razon, :status, :creado_por, :actualizado_por, :creado, :actualizado, :created_at, :updated_at
json.url detallecaja_url(detallecaja, format: :json)
